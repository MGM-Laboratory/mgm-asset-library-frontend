name: E2E

# Full Playwright suite lands in Part 4. This file is wired up so the
# workflow exists in `main` and can be extended without restructuring.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "E2E suite ships in Part 4."
